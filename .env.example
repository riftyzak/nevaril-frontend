APP_DATA_SOURCE=convex
NEXT_PUBLIC_APP_DATA_SOURCE=convex
NEXT_PUBLIC_CONVEX_URL=
CONVEX_URL=
# M27 keeps mock auth as the safe default. Switch both auth envs to convex
# only when you want the seeded backend auth handoff.
AUTH_SOURCE=mock
NEXT_PUBLIC_AUTH_SOURCE=mock
